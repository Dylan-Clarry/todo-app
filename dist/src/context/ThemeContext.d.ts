import React from "react";
declare type ThemeContextProps = {
    children: React.ReactNode;
};
export declare const ThemeContextProvider: ({ children }: ThemeContextProps) => JSX.Element;
export {};
