import React from 'react';

const Form = () => {
    return(
        <form>
            <input
                type="text"
                className="form-input"
            />
            <button
                type="submit"
            >+</button>
        </form>
    );
};

export default Form;
